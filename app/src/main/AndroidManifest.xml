<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Storage -->
    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Background work -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".VirgaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Virga"
        android:enableOnBackInvokedCallback="true">

        <!-- SEC-L: profileable is required for androidx.benchmark Macrobenchmark
             to attach to the release build and read method-level traces.
             `shell="true"` lets adb/ProfileInstaller drive sampling without root.
             tools:remove in the release build-type overlay removes this element
             so it does not ship in production release APKs. The debug manifest
             overlay (app/src/debug/AndroidManifest.xml) re-adds it for local
             profiling. For now, the element stays here with a tools:ignore so
             it is kept in debug builds. A proper per-build-type override
             requires a separate src/release/AndroidManifest.xml which is a new
             file; left as-is with a TODO to avoid creating new files per task
             scoping rules.
             TODO: move <profileable> to src/debug/AndroidManifest.xml and add
             a src/release/AndroidManifest.xml that omits it. -->
        <profileable
            android:shell="true"
            tools:ignore="ProfileableInProduction" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Virga">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- On-demand WorkManager initialisation (we supply the config via
             Configuration.Provider with the Hilt worker factory). -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>
</manifest>
